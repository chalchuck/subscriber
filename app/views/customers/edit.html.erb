<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3>Update your customer's details</h3>
      </div>

      <div class="panel-body">
      	<%= simple_form_for([@current_business, @current_customer]) do |f| %>
      	  <%= render 'form', f: f %>
      	  <%= render "form_buttons", placeholder: "customer" %>
      	<% end %>
      </div>
    </div>
  </div>
</div>
